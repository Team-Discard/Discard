namespace Uxt
{
    /// <summary>
    /// An empty struct that serves as a "void" generic type parameter.
    /// This is a workaround since C# doesn't allow void as a type
    /// parameter.
    /// </summary>
    public struct Void
    {
    }
}